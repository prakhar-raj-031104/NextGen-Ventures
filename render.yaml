services:
  # ── Backend API (Node.js / Express) ────────────────────────────────────────
  - type: web
    name: nextgen-api
    env: node
    region: singapore
    buildCommand: npm ci && npm run build:prod -w apps/backend
    startCommand: npm run start:prod -w apps/backend
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: CORS_ORIGIN
        value: https://nextgen-web.onrender.com

  # ── Frontend Static Site (React / Vite) ────────────────────────────────────
  - type: web
    name: nextgen-web
    env: static
    buildCommand: npm ci && npm run build -w apps/frontend
    staticPublishPath: apps/frontend/dist
    envVars:
      - key: VITE_API_URL
        value: https://nextgen-api.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
